# 🚀 ML-Notes  
- This repo contains some models that I have trained  
- I use this as a reference when developing other models  

## 📑 Table of contents
- 🚗 [Car Price Predictor](car_price_neural_network/car_price_predictor.ipynb): simple neural network to predict prices of used cars

### 📦 Requirements
Make sure you have Python 3.8+ installed. You can install the dependencies with:

```bash
pip install -r requirements.txt